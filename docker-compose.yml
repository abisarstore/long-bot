services:
  api:
    build: .
    command: uvicorn api_server:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file: .env

  worker:
    build: .
    command: python main_worker.py
    env_file: .env

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
